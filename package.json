{
  "name": "friends",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --open --host",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && vite preview --host --open",
    "deploy": "vercel --prod"
  },
  "typeRoots": [
    "node_modules/@types",
    "src/@types"
  ],
  "devDependencies": {
    "@types/stats.js": "^0.17.3",
    "lil-gui": "^0.19.1",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "babylonjs": "^6.33.1",
    "howler": "^2.2.4",
    "stats.js": "^0.17.0",
    "vercel": "^33.0.1",
    "vite": "^5.0.8"
  }
}
